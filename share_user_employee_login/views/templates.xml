<odoo>

    <template id="employee_login_form" name="Employee Login Form">
        <t t-call="web.login_layout">
            <t t-set="disable_footer">1</t>
            <div class="oe_login_form">
                <h5 class="card-title">Employee Authentication</h5>
                <form method="POST" action="" class="">
                    <div class="mb-2 mt-2">
                        <h5 class="card-title" style="font-size: medium">Let the system know who you are.</h5>
                    </div>
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <input type="hidden" name="redirect" t-att-value="redirect"/>

                    <div class="oe_login_form_container">

                        <!-- Error -->
                        <t t-if="error">
                            <div class="alert alert-danger">
                                <t t-esc="error"/>
                            </div>
                        </t>

                        <!-- Employee dropdown -->
                        <div class="mb-3">
                            <label class="form-label">
                                Employee
                            </label>

                            <select name="employee_id" class="form-select" required="required">
                                <option value="">
                                    Select employee...
                                </option>

                                <t t-foreach="shared_employee_ids" t-as="emp">
                                    <option t-att-value="emp.id">
                                        <t t-esc="emp.name"/>
                                        <t t-if="emp.job_title">
                                            (<t t-esc="emp.job_title"/>)
                                        </t>
                                    </option>
                                </t>

                            </select>
                        </div>

                        <!-- Code input -->
                        <div class="mb-3">
                            <label class="form-label">
                                PIN Code
                            </label>

                            <input type="password"
                                   name="code"
                                   class="form-control"
                                   placeholder="Enter code"
                                   required="required"
                                   autofocus="autofocus"/>
                        </div>

                        <!-- Login button -->
                        <div class="d-grid">
                            <button type="submit"
                                    class="btn btn-primary btn-block">

                                Login

                            </button>
                        </div>

                    </div>

<!--                    <div class="mb-3">-->
<!--                        <label class="fw-bold" for="totp_token">Authentication Code</label>-->
<!--                        <input id="totp_token" name="totp_token" class="form-control mb-2"-->
<!--                                autocomplete="off" inputmode="numeric" autofocus="autofocus" required="required" placeholder="e.g. 123456"/>-->
<!--                    </div>-->
<!--                    <div class="text-center py-2 border-top">-->
<!--                        <p class="alert alert-danger" t-if="error" role="alert">-->
<!--                            <t t-esc="error"/>-->
<!--                        </p>-->
<!--                    <div class="mb-2 mt-2 text-muted">-->
<!--                        <input type="checkbox" name="remember" id="switch-remember" value="1"/>-->
<!--                        <label for="switch-remember">Don't ask again on this device</label>-->
<!--                    </div>-->
<!--                        <div t-attf-class="clearfix oe_login_buttons text-center d-grid mb-1">-->
<!--                            <button type="submit" class="btn btn-primary">-->
<!--                                Log in-->
<!--                            </button>-->
<!--                        </div>-->

<!--                    </div>-->
                </form>

                <form method="POST" action="/web/session/logout">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <div class="w-100 text-center">
                        <button type="submit" class="btn btn-link btn-sm mb-2">
                            Cancel
                        </button>
                    </div>
                </form>
            </div>
        </t>
    </template>
</odoo>
